# Отчет о выполнении Лабораторной работы №5Д

## Вариант 5: Треугольник (позиции 4, 5, 6)

---

## 1. ЗАДАНИЕ

Добавить в программу фигуру **Треугольник** (№8 из таблицы) в позициях:
- **Позиция 4** — левое ухо
- **Позиция 5** — правое ухо  
- **Позиция 6** — кокарда на шляпе

**Характеристики фигуры:**
- Отражение: ДА
- Поворот: ДА

---

## 2. ВЫПОЛНЕННАЯ РАБОТА

### 2.1. Создан класс Triangle

**Файл:** `src/triangle.h`

**Наследование:**
```cpp
class Triangle : public Shape, public Rotatable, public reflectable
```

**Структура класса:**
- 3 защищенных поля типа `Line*` (стороны треугольника)
- Конструктор, принимающий 3 точки (вершины)
- Виртуальный деструктор с освобождением памяти
- Реализация всех чисто виртуальных методов базовых классов

### 2.2. Реализованные методы

#### От класса Shape:
✅ `void draw(Screen* screen) const` - отрисовка треугольника  
✅ `void move(Point p)` - перемещение треугольника  
✅ `Point getLeftTop() const` - левая верхняя габаритная точка  
✅ `Point getRightTop() const` - правая верхняя габаритная точка  
✅ `Point getLeftBottom() const` - левая нижняя габаритная точка  
✅ `Point getRightBottom() const` - правая нижняя габаритная точка  

#### От класса Rotatable:
✅ `void rotateLeft()` - заглушка  
✅ `void rotateRight()` - заглушка  

#### От класса reflectable:
✅ `void flipHorisontally()` - заглушка  
✅ `void flipVertically()` - заглушка  

### 2.3. Модифицирован main.cpp

Добавлены три треугольника:

#### Левое ухо (позиция 4):
```cpp
Point earLeftP1(earLeftTop.getX() - 4, earLeftTop.getY() + 2);
Point earLeftP2(earLeftTop.getX(), earLeftTop.getY() + 2);
Point earLeftP3(earLeftTop.getX() - 2, earLeftTop.getY() - 2);
shapes.emplace_back(std::make_shared<Triangle>(earLeftP1, earLeftP2, earLeftP3));
```

#### Правое ухо (позиция 5):
```cpp
Point earRightP1(earRightTop.getX(), earRightTop.getY() + 2);
Point earRightP2(earRightTop.getX() + 4, earRightTop.getY() + 2);
Point earRightP3(earRightTop.getX() + 2, earRightTop.getY() - 2);
shapes.emplace_back(std::make_shared<Triangle>(earRightP1, earRightP2, earRightP3));
```

#### Кокарда (позиция 6):
```cpp
Point kokardaP1(hatCenter.getX() - 2, hatCenter.getY() + 2);
Point kokardaP2(hatCenter.getX() + 2, hatCenter.getY() + 2);
Point kokardaP3(hatCenter.getX(), hatCenter.getY() - 1);
shapes.emplace_back(std::make_shared<Triangle>(kokardaP1, kokardaP2, kokardaP3));
```

### 2.4. Созданы вспомогательные файлы

Созданы все необходимые заголовочные файлы базовых классов:
- `src/point.h` - класс Point
- `src/screen.h` - класс Screen  
- `src/shape.h` - базовый абстрактный класс Shape
- `src/rotatable.h` - интерфейс Rotatable
- `src/reflectable.h` - интерфейс reflectable
- `src/line.h` - класс Line
- `src/square.h` - класс Square

---

## 3. ОСОБЕННОСТИ РЕАЛИЗАЦИИ

### 3.1. Множественное наследование
Класс Triangle наследуется от трех базовых классов одновременно:
- Shape (основной абстрактный класс)
- Rotatable (для возможности поворота)
- reflectable (для возможности отражения)

### 3.2. Вычисление габаритных точек
Используются функции `std::min` и `std::max` для нахождения крайних координат:

```cpp
std::uint32_t minX = std::min({
  _side1->getLeftTop().getX(),
  _side2->getLeftTop().getX(),
  _side3->getLeftTop().getX()
});
```

### 3.3. Управление памятью
- Внутри класса Triangle используются сырые указатели (`Line*`)
- В деструкторе вызывается `delete` для каждой стороны
- В main.cpp объекты создаются через `std::shared_ptr` для автоматического управления памятью

### 3.4. Виртуальные функции
Все методы объявлены как `virtual` с спецификатором `override` для:
- Корректной работы полиморфизма
- Обеспечения динамического связывания
- Компиляционной проверки перегрузки методов

---

## 4. РЕЗУЛЬТАТЫ РАБОТЫ

### 4.1. Компиляция
```bash
$ make
🤖 Компиляция src/main.cpp...
🔗 Создание ссылок
🗒️ Сборка документации
```
✅ **Компиляция прошла успешно**

### 4.2. Запуск программы
```bash
$ make run
```

**Результат:**
```
               ******************                 
               *                *                 
               *       *        *                 
               *      * *       *                 
               *      * *       *                 
               *     *****      *                 
               *                *                 
               *                *                 
               *                *                 
            *  ******************  *              
           *************************              
           * ********************** *             
          *  **                  *  *             
          ***** ***          *** *****            
              *                  *                
              *        *         *                
              *                  *                
              *                  *                
              * **************** *                
              *                  *                
              ********************                
                                                  
                ****************                  
                *              *                  
```

✅ **Программа работает корректно**

На картинке видны:
- Шляпа с кокардой (треугольник в центре)
- Уши (два треугольника по бокам головы)
- Голова с глазами, носом и ртом
- Тело

---

## 5. ПРОВЕРКА ТРЕБОВАНИЙ

| Требование | Статус | Комментарий |
|-----------|--------|-------------|
| Класс Triangle создан | ✅ | src/triangle.h |
| Наследование от Shape | ✅ | `class Triangle : public Shape` |
| Наследование от Rotatable | ✅ | `class Triangle : public Rotatable` |
| Наследование от reflectable | ✅ | `class Triangle : public reflectable` |
| Метод draw() | ✅ | Рисует 3 линии |
| Метод move() | ✅ | Перемещает 3 стороны |
| Методы get*() | ✅ | Все 4 метода реализованы |
| Методы rotate*() | ✅ | Заглушки |
| Методы flip*() | ✅ | Заглушки |
| Деструктор | ✅ | Удаляет 3 линии |
| Позиция 4 (левое ухо) | ✅ | Треугольник слева |
| Позиция 5 (правое ухо) | ✅ | Треугольник справа |
| Позиция 6 (кокарда) | ✅ | Треугольник на шляпе |
| Использование std::min/max | ✅ | Для габаритных точек |
| Комментарии на русском | ✅ | Подробные комментарии |
| Компиляция без ошибок | ✅ | make успешно |
| Нет утечек памяти | ✅ | delete в деструкторе |

---

## 6. СТРУКТУРА ФАЙЛОВ ПРОЕКТА

```
oop_lab_5d/
├── src/
│   ├── main.cpp          # Основная программа с треугольниками
│   ├── triangle.h        # Класс Triangle (НОВЫЙ)
│   ├── point.h           # Класс Point
│   ├── screen.h          # Класс Screen
│   ├── shape.h           # Базовый класс Shape
│   ├── rotatable.h       # Интерфейс Rotatable
│   ├── reflectable.h     # Интерфейс reflectable
│   ├── line.h            # Класс Line
│   └── square.h          # Класс Square
├── build/
│   └── main              # Исполняемый файл
├── Makefile              # Сборка проекта
└── TRIANGLE_DOC.md       # Документация
```

---

## 7. ВЫВОДЫ

1. **Успешно реализован** класс Triangle с множественным наследованием от трех базовых классов.

2. **Применены принципы ООП:**
   - Инкапсуляция (защищенные поля)
   - Наследование (от Shape, Rotatable, reflectable)
   - Полиморфизм (виртуальные функции)

3. **Правильное управление памятью:**
   - Деструктор освобождает выделенную память
   - Использование умных указателей в main.cpp

4. **Все требования задания выполнены:**
   - Три треугольника добавлены в нужных позициях
   - Программа компилируется и работает
   - Код содержит подробные комментарии

5. **Изучены концепции:**
   - Виртуальные функции
   - Абстрактные классы
   - Множественное наследование
   - Габаритные прямоугольники

---

**Дата выполнения:** 13 ноября 2025  
**Язык программирования:** C++23  
**Компилятор:** g++
